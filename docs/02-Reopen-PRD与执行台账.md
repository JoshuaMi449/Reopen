# Reopen（VC复活点）PRD 与执行台账

> 本文件是 Reopen 项目的**需求文档 + 执行台账**（双文档机制中的文档 B）。所有产品决策来自 2026-08-18~19 与用户的深度产品交流（13+ 轮逐项确认）。
> 通用知识（PRD 是什么、名词解释等）见 [知识库-开发大脑](知识库-开发大脑/README.md)。

---

## 一、产品概述

| 项                            | 内容                                                                                                                         |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 产品名                        | **Reopen**（界面与仓库统一只显示 Reopen；中文别称"VC复活点"仅用于文档与介绍）                                                |
| 一句话简介（GitHub 仓库描述） | Restart your Mac without losing your projects                                                                                |
| 痛点                          | 重启 Mac 后，vibecoding 产物（localhost 服务、散落的 html 文件）"丢失"——服务不在了、文件找不到了、自制启动器想不起来在哪     |
| 解决方案                      | 一个桌面应用：**登记你的项目 → 一键启动/停止 → 查看状态与日志 → 打开 Reopen 自动复活自启项**。既是管理器，也是自动化工具     |
| 形态                          | macOS 窗口应用，dmg 包分发，开源发布 GitHub（`JoshuaMi449/Reopen`，MIT 协议），未来兼容 Windows                              |
| 技术栈                        | Electron + React + TypeScript（选型理由：TypeScript 是 GitHub 第一大语言、AI 编写最熟练、跨平台 dmg+exe 一条链路、生态成熟） |

**核心概念**：两层"自动"机制

- **开机自启（电脑层）**：Reopen 自己加入 Mac 登录项，开机自动打开
- **自启项（软件层）**：打开 Reopen 后，自动启动你放进"自启项"里的项目
- 两层叠加 = 全自动复活链：Mac 开机 → Reopen 打开 → 项目全部自动拉起

## 二、目标用户

| 用户       | 描述                                                                                      |
| ---------- | ----------------------------------------------------------------------------------------- |
| 主要用户   | 用 AI 做开发（vibe coding）的非专业程序员——自己做的项目散落在电脑各处，重启后需要逐个找回 |
| 开源后扩展 | 有同样痛点的独立开发者、尝鲜用户                                                          |

## 三、功能需求

> 第一版**全功能一次做完**（用户拍板：不分批）。以下所有功能均为 P0。

### 3.1 项目类型（两种）

| 类型         | 定义                                        | 管理能力                          |
| ------------ | ------------------------------------------- | --------------------------------- |
| **本地服务** | localhost 服务（如 `npm run dev` 起的网站） | 启动命令、端口、日志、状态        |
| **网页文件** | 散落的 html 文件/文件夹                     | 自动起临时 http 服务 + 浏览器打开 |

> 「应用」类型（.app 文件）第一版**砍掉**（用户拍板）。原因：.app 天生不丢（启动台/桌面躺着）、无管理点（无命令/端口/日志）、进程监控复杂。拖入 .app 时的兜底行为见 3.2。

### 3.2 添加项目（拖拽自动识别）

| 行为                   | 规则                                                                                                            |
| ---------------------- | --------------------------------------------------------------------------------------------------------------- |
| 拖入**文件夹**         | 识别：含 package.json → 本地服务；含 html 文件 → 网页文件                                                       |
| 拖入**单个 html 文件** | 识别为网页文件                                                                                                  |
| 拖入 **.app 文件**     | 提示"第一版不支持应用类型" → 询问是否解析 → 是服务/网页包装器则转成对应类型登记；解析不出提示失败               |
| 识别失败/猜错          | 弹确认表单，类型预选可改，永不卡死                                                                              |
| 表单自动填写           | **全自动猜**：名称取文件/文件夹名、路径自动填、启动命令从 package.json scripts 猜、端口自动猜——你只需确认或修改 |

**确认表单字段**：

| 字段           | 本地服务          | 网页文件            |
| -------------- | ----------------- | ------------------- |
| 名称           | 自动猜            | 自动猜              |
| 路径           | 自动填            | 自动填              |
| 启动命令       | 自动猜（scripts） | —（自动起临时服务） |
| 端口           | 自动猜            | 可指定（留空自动分配） |
| 启动后开浏览器 | 滑动按钮，默认关  | 滑动按钮，默认关    |
| 备注           | 手填              | 手填                |
| 标签           | 手填/选           | 手填/选             |

### 3.3 主界面（侧边栏 + 列表 / 卡片，双视图）

**侧边栏**：全部 / 最近使用 / 服务 / 网页 / 标签（分类浏览）

**列表行**（默认视图）：状态圆点（🟢运行/⚪停止）+ 名称 + 端口号 + 上次启动时间 + 类型图标 + 备注 + 启动/停止按钮

**卡片视图**：可切换，同数据不同排版

**顶部工具栏**：搜索框 + 标签（访达式彩色标签分类）+ 视图切换 + 「+」添加按钮 + 设置齿轮

**排序**：访达式三选一随时切换——手动拖拽 / 最近使用 / 名称

**右键菜单**（列表行/卡片）：启动 / 停止 / 在浏览器打开 / 编辑 / 删除 / 加入自启项。删除弹确认框，文案强调"**只从 Reopen 移除登记，不删你电脑上的原文件**"

**明确没有**：「全部启动」按钮（用户拍板不要——复活靠自启项自动拉 + 菜单栏逐个点）

### 3.4 启动 / 停止 / 状态与日志

| 项       | 行为                                                                                                             |
| -------- | ---------------------------------------------------------------------------------------------------------------- |
| 启动     | 点行尾按钮；服务类起进程，网页类起临时服务                                                                       |
| 停止     | 停掉对应进程（网页类杀掉临时服务）                                                                               |
| 启动失败 | **不重试**；卡片/行标红 + 右上角通知**带失败原因**（如"端口3459已被占用"），点通知展开该项目的日志面板看完整错误 |
| 日志查看 | **行内展开面板**（点列表行展开）：状态、端口、命令、实时日志、停止/重启/编辑按钮                                 |

### 3.5 自启项（软件层自动启动）

| 项       | 行为                                                                                  |
| -------- | ------------------------------------------------------------------------------------- |
| 总开关   | 偏好设置→通用里，**默认开启**，旁边白话小字解释：_打开软件后，自动启动你放进来的产品_（关闭=没有这个功能，顶部 icon 不显示） |
| 开启后   | 主界面上方多出一个 icon，点开**气泡面板**（浮在主界面上方的小面板）                   |
| 加入方式 | 把列表行**拖进气泡面板**即加入                                                        |
| 同步显示 | 管理所有项目的界面里，在自启项里的项目**打勾**同步显示                                |
| 触发     | 打开 Reopen 时自动启动面板内所有项目                                                  |

### 3.6 偏好设置（6 组，Mac 标准式：左侧分组 + 右侧内容）

| 分组       | 设置项                                                                                                                |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| **通用**   | 自启项总开关（白话解释）、开机自启（Reopen 加入 Mac 登录项）、关闭时最小化到托盘、默认浏览器、语言（中/英）、通知方式 |
| **外观**   | 主题配色、浏览（列表间距/卡片大小调整）                                                                               |
| **菜单栏** | 启用开关（**默认开启**）、样式自定义                                                                                  |
| **快捷键** | 全局唤起窗口（默认 **⌥+R**，可改）、快捷启动项目（**不设默认**，自己绑）、应用内快捷键（⌘N 添加/⌘F 搜索等）           |
| **资料库** | 所有产品的源文件路径一览、数据备份导出/导入                                                                           |
| **关于**   | 版本号、检查更新、GitHub 地址、开源协议                                                                               |

### 3.7 菜单栏

- 默认开启（Mac 顶栏小图标）
- 点击弹出小面板：**项目列表快速启停**（带状态圆点，点一下启/停）+ 打开主窗口 / 退出
- 不放「全部启动」按钮

### 3.8 主题配色（借鉴 Proma 机制 + 自定义色值）

| 风格               | 浅色                                               | 深色                                                 |
| ------------------ | -------------------------------------------------- | ---------------------------------------------------- |
| **莫兰迪（默认）** | 晨雾暖沙：背景 #ece8e2、主色 #c0a29a               | 莫兰迪夜：背景 #1e1b20、主色 #c9a89e                 |
| 海洋               | 晴空：背景 #ecf2f7、主色 #408abf                   | 深海：背景 #141a21、主色 #5c85a8                     |
| 石墨               | 晨雾：背景 #e9e9e6、主色 #4a4a45、强调琥珀 #b98a4a | 石墨夜：背景 #1a1a18、主色 #8a8a82、强调琥珀 #d9a45b |

- 亮暗默认**跟随系统**，可手动锁
- 机制参考 Proma（3 风格 × 亮暗 = 6 套），色值重新定制避免撞脸

### 3.9 新手引导（Onboarding / Guided Tour）

- 流程：打开 → **欢迎界面**（介绍+开始按钮）→ 进入主界面后**主界面变暗（蒙层）** → **弹出引导窗口分步讲解** → 上一步/下一步/跳过按钮
- 引导步骤（4 步 + 收尾）：
  1. 指向侧边栏——「按类型和标签浏览你的项目」
  2. 指向列表行——「点一下启动/停止，点开行内面板看日志」
  3. 指向顶部「+」——「把项目文件夹或 html 拖进窗口，自动识别登记」
  4. 指向自启项 icon——「拖进去，以后打开 Reopen 自动复活」
  5. 收尾——「⌥+R 随时唤出窗口」
- **不放示例项目**（用户拍板：引导教完直接拖真实项目，列表干净）

### 3.10 图标与文案

| 项       | 要求                                                                                                                                         |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| App 图标 | 抽象、高级感；macOS 原生风：圆角矩形（Squircle）底 + 同色系柔和渐变 + 白色极简图形；不花哨；AI 生成候选后挑选（统一走 lanzhuo-imageee 生成） |
| 界面图标 | 统一用图标库：**Lucide**（lucide-react）优先，AI 相关场景用 **Lobe Icons**（@lobehub/icons）；不用 emoji 当功能图标                          |
| 显示名   | 界面/菜单栏/关于页统一只显示 **Reopen**                                                                                                      |

## 四、非功能需求

| 类别   | 要求                                                                                       |
| ------ | ------------------------------------------------------------------------------------------ |
| 性能   | 应用启动 ≤ 3 秒；列表滚动流畅；日志实时刷新不卡界面                                        |
| 兼容   | macOS 12 以上（待验证后确定）；预留 Windows 结构（Electron 天然跨平台）                    |
| 数据   | 项目清单存 JSON 文件，位置 `~/Library/Application Support/Reopen/`；不联网、不上传任何数据 |
| 安全   | 存储的启动命令只在本机执行；不收集用户信息                                                 |
| 稳定性 | 单例锁（防双开）；主进程保持轻量；项目进程与应用进程分离（项目挂了不影响 Reopen）          |

## 五、验收标准（每条可验证）

| #   | 验收标准                                                                           |
| --- | ---------------------------------------------------------------------------------- |
| 1   | 把一个含 package.json 的文件夹拖进窗口，自动识别为本地服务，名称/路径/命令自动填好 |
| 2   | 点启动后，服务在 30 秒内就绪，状态圆点变绿，行内面板能看到实时日志                 |
| 3   | 点停止后，对应进程被完全终止（端口释放，可再次启动）                               |
| 4   | 拖入一个 html 文件，识别为网页文件，启动后自动起临时服务并在默认浏览器打开         |
| 5   | 启动失败时，行标红 + 通知显示具体失败原因                                          |
| 6   | 拖入 .app 文件时，提示不支持并询问是否解析                                         |
| 7   | 删除项目时弹确认框，确认后仅移除登记，原文件完好                                   |
| 8   | 开启自启项开关后顶部出现 icon；拖入项目后主界面列表该行打勾                        |
| 9   | 打开 Reopen 时，自启项内项目自动启动                                               |
| 10  | 偏好设置 6 组齐全，主题三风格×亮暗切换即时生效                                     |
| 11  | 菜单栏默认显示图标，面板可快速启停项目                                             |
| 12  | ⌥+R 可在任何软件里唤出 Reopen 窗口                                                 |
| 13  | 首次打开出现欢迎页+蒙层分步引导，可上一步/下一步/跳过；第二次打开不再出现          |
| 14  | 重启 Mac 后（登录项开启时），Reopen 自动打开并拉起自启项项目                       |
| 15  | 三种排序方式（手动拖拽/最近使用/名称）切换后列表顺序正确变化                       |

## 六、里程碑（技术侧拆分）

| 里程碑          | 内容                                                                                          | 完成标志                         |
| --------------- | --------------------------------------------------------------------------------------------- | -------------------------------- |
| **M1 项目骨架** | 初始化 Electron+React+TS 工程、GitHub 仓库、双文档入仓                                        | 空窗口能跑，`npm run dev` 出界面 |
| **M2 核心能力** | 项目登记（拖拽识别+表单）、JSON 存储、启停进程、状态与日志面板                                | 验收标准 1-7 通过                |
| **M3 完整体验** | 自启项、菜单栏、偏好设置 6 组、三主题、双视图、标签、排序、快捷键、Onboarding、单例锁、登录项 | 验收标准 8-15 通过               |
| **M4 发布**     | 图标、打包 dmg、签名策略（自签名+说明文档）、README 中英双语、GitHub Release + 自动更新       | 用户能下载 dmg 安装使用          |

## 七、明确不做（第一版）

| 不做                      | 说明                                    |
| ------------------------- | --------------------------------------- |
| 「应用」类型              | 已砍（见 3.1），.app 拖入有兜底解析流程 |
| 自动扫描/白名单           | 只管理手动登记的项目；扫描发现功能后置  |
| 示例项目                  | 引导教完直接拖真实项目                  |
| 「全部启动」按钮          | 复活靠自启项+菜单栏                     |
| md 文档管理、CLI 脚本管理 | 不在第一版范围                          |

## 八、技术方案概要（技术侧拍板）

| 项       | 决策                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------ |
| 架构     | Electron 双进程：主进程管系统交互（进程管理、文件、菜单栏/托盘、登录项），渲染进程 React 画界面；界面层/逻辑层分离 |
| 模块划分 | 项目管理 / 进程启动 / 端口检测（健康检查） / 配置存储（JSON） / 界面，五模块                                       |
| 进程管理 | 服务类用 child_process 拉起并记录 PID 与进程树，停止时整树终止；网页类起临时 http server，端口自动分配             |
| 端口检测 | 启动后轮询端口就绪（健康检查），超时判失败并捕获错误输出作为"失败原因"                                             |
| 存储     | 项目清单 projects.json + 设置 settings.json，存 `~/Library/Application Support/Reopen/`                            |
| 打包     | electron-builder 出 dmg；electron-updater 检查 GitHub Releases 更新                                                |
| 系统集成 | 单例锁、launchd 登录项（开机自启开关）、托盘/菜单栏、全局快捷键（⌥+R）                                             |
| 质量     | TypeScript 严格模式 + ESLint + Prettier；单元测试覆盖核心模块；GitHub Actions 自动构建                             |

## 九、执行台账（做一步记一步）

### 9.1 已完成

| 日期       | 事项                                                                                                                                                       | 产出                                         |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 2026-08-18 | 立项：痛点定义、GitHub 同款调研（无完全同类）、技术栈选型（Electron+React+TS）、名称与目录确定                                                             | 产品方向确定                                 |
| 2026-08-18 | 环境检查与准备：Node/npm/Git/Homebrew 就绪，安装 gh 并登录（JoshuaMi449）                                                                                  | 开发环境就绪                                 |
| 2026-08-18 | 文档 A（产品开发知识库·开发大脑）撰写完成                                                                                                                  | 15 章知识库                                  |
| 2026-08-19 | 产品深度交流 13+ 轮：功能清单、交互细节、偏好设置、主题配色、引导流程、GitHub 发布全部逐项确认                                                             | 本 PRD 全部决策                              |
| 2026-08-19 | 文档 A 两轮深度自查：补 65 词条、16 语言词条、修 7 事实错误、10 重复、10 归类错位                                                                          | 知识库补全                                   |
| 2026-08-19 | 知识网络化改造：拆 25 文件 + 双链互跳 + 阅读地图 + 学习路线，零丢失校验通过，两轮结构/内容终审干净                                                         | [知识库-开发大脑](知识库-开发大脑/README.md) |
| 2026-08-19 | 文档 B（本文件）初版完成                                                                                                                                   | PRD+台账                                     |
| 2026-08-19 | M1 完成：git 初始化、electron-vite 工程（Electron 39 + React 19 + TS）、依赖安装、GitHub 仓库创建并推送（61 文件）、npm run dev 空窗口跑通（用户肉眼确认） | https://github.com/JoshuaMi449/Reopen        |
| 2026-08-20 | M2 完成：拖拽识别（四层端口读取）、确认表单（标签点选已有）、启停（30 秒健康检查+整树终止）、日志面板、网页临时服务（端口可指定）、失败标红+人话原因、接管运行中项目、右键菜单、删除确认、.app 解析、查重；验收 1-7 实测通过 | 功能可用 |

### 9.2 待办

- [x] M1：初始化 Electron+React+TS 工程，创建 GitHub 仓库 JoshuaMi449/Reopen
- [x] M1：双文档入仓（知识库目录 + 本 PRD）
- [x] M2：项目登记（拖拽识别、表单、全自动猜）
- [x] M2：JSON 存储、启停进程、状态与日志面板
- [ ] M3：自启项、菜单栏、偏好设置 6 组、三主题、双视图、标签、排序、快捷键、Onboarding、单例锁、登录项
- [ ] M4：App 图标生成候选并挑选（lanzhuo-imageee）
- [ ] M4：打包 dmg、README 中英双语、GitHub Release 发布

### 9.3 变更日志

| 日期       | 变更                                                                                                    | 原因/来源    |
| ---------- | ------------------------------------------------------------------------------------------------------- | ------------ |
| 2026-08-19 | 新增「自启项」功能（软件层自动启动，区别于电脑层登录项）                                                | 用户新需求   |
| 2026-08-19 | 「应用」类型砍掉                                                                                        | 用户产品判断 |
| 2026-08-19 | 取消「全部启动」按钮                                                                                    | 用户拍板     |
| 2026-08-19 | 示例项目改为不做（引导教完直接拖真实项目）                                                              | 用户产品判断 |
| 2026-08-19 | 名称"项目组"改为"自启项"（偏好设置内配白话解释）                                                        | 用户命名要求 |
| 2026-08-19 | 网页文件端口改为表单可指定（留空自动分配） | 用户反馈 |
| 2026-08-19 | 右键菜单新增「在浏览器打开」；表单"启动后打开浏览器"开关保留（默认关） | 用户反馈 |
| 2026-08-19 | 标签支持点选已有标签 + 手填新增 | 用户反馈 |
| 2026-08-19 | 拖入已登记过的路径 → 提示"已经登记过了"，不弹表单（查重） | 用户反馈 |
| 2026-08-19 | 端口读取四层策略：源码写死 → .env 文件 → vite/webpack 配置 → 框架默认；都读不到表单留空待填（不瞎猜） | 用户反馈（开源覆盖全面性） |
| 2026-08-20 | 已在运行的项目直接接管显示（端口有响应即绿点+端口号，不用先杀再开）；点启动遇端口占用改接管；点停止杀端口监听者 | 用户拍板 |
| 2026-08-19 | 启动失败原因翻译人话（如"端口 3459 已被占用——是不是之前已经手动启动了？"） | 实测反馈 |
| 2026-08-19 | 界面图标决策：功能图标用 Lucide（lucide-react）优先、AI 场景用 Lobe Icons（@lobehub/icons），不用 emoji | 用户追加     |

---

_本台账规则：每完成一步，在 9.1 加一行；每新增/修改决策，在 9.3 加一行。_

| 2026-08-20 | 自启项总开关改为默认开启；关闭时顶部 icon 不显示 | 用户验收纠正 |
| 2026-08-20 | 引导整改：引导卡移到介绍区域外 + 三角箭头指向高亮区 | 用户验收纠正 |
| 2026-08-20 | 托盘加右键菜单（打开主窗口/偏好设置/退出） | 用户验收纠正 |
| 2026-08-20 | 外观整改：删「主题风格」三卡与「紧凑间距」；特殊风格改名（雾白/天青/苔绿/山霭/林夜/赭夜/磷光）；配色改多色搭配（主题色+辅助色 accent，预览卡改聊天气泡拼贴）；设置页 Telegram 式（行间细分隔线+窄侧栏） | 用户验收纠正 |
| 2026-08-20 | 主界面改 Proma 式三栏：左分类 190px 固定、中列表弹性、右详情 380px 固定（点击项目滑出、中间收缩，不再浮层遮挡）；三栏背景分层（左最浅→中→右卡片色）；窗口默认宽 1120 可调 | 用户拍板 |
| 2026-08-20 | 三栏细化：三块区域改圆角卡片、四边 8px 间隙浮在背景上（Proma classic 式）；搜索栏顶部通栏；右栏与中间齐平不占搜索栏行；右栏滑出改宽度动画（丝滑不闪） | 用户拍板 |
| 2026-08-20 | 自启项面板删全屏遮罩（原遮罩挡住列表导致行拖不进面板）；顶部加标题行：红黄绿按钮右侧放 Reopen logo（卡片不再侵占按钮区） | 实测反馈 |
| 2026-08-20 | 第二轮 UI 整改（苏格拉底式两轮确认）：窗口最小尺寸 980×620；左栏贴边到顶（按钮区留空、无圆角间隙），中右卡片加 Proma 式下阴影；列表间距四边统一 16px；左栏删「最近使用」、底部 logo（纯展示）+设置入口、右上角齿轮删除；搜索收起式（点 icon/⌘F 展开，Esc/再点 icon/失焦收起）；排序体系重做=名称/最近打开/添加日期/标签/——/无（默认最近打开，无=手动拖拽）；标签排序带分组组头；工具栏 icon 悬停 1.5s 气泡说明；自启面板简化（无开关无✕）定位 icon 下方右对齐，Esc/点外部/再点 icon 关闭（拖拽期间不关） | 用户拍板 |
| 2026-08-20 | toolbar 归入中间栏（右栏滑出时随中间整体挤压，toolbar 去卡片化改分隔线式）；「+」按钮改为打开访达选文件夹→自动识别→确认表单补信息（IPC dialog:pick-project-folder）；悬停气泡提速 0.6s 且显示在 icon 下方不超窗；侧栏 Reopen 文字换 HFPoohBear-2.ttf 字体（用户素材入仓） | 用户拍板 |
| 2026-08-20 | 侧栏 Reopen 文字改为渲染透明 PNG（Swift 用 HFPoohBear 渲染 wordmark.png，不打包字体文件，深色主题 CSS invert）；自启面板智能方向（右侧空间足往右开不挡列表）；Tooltip 改原生事件修复 select 悬停不显示 | 实测反馈 |
| 2026-08-20 | 项目移动找回：右键菜单「重新定位…」→ 访达选新位置（网页项目可选文件）→ 更新路径；启动时路径不存在仍标红人话提示 | 用户提问 |
| 2026-08-20 | Logo 参考图尝试（RP 字母标 AI 生成 4 版）效果不达用户要求，**放弃 AI 生成方向** | 用户拍板 |
| 2026-08-20 | 悬停气泡提速 0.3s；侧栏底部加 no-drag（修复设置点不动/hover 无方块——drag 区吞事件）；wordmark 恢复 20px；排序删「———」分隔；最小宽度固定 1100（日志滑入后中间保 2 列，窗口不跳） | 实测反馈 |
| 2026-08-20 | 卡片网格动态列宽（ResizeObserver）：整数列+居中左右等距；minWidth 1100 时开日志=2列×225、关日志=4列×204；窗口任意宽度列数自适应 | 用户拍板 |
| 2026-08-20 | 六项整改：①删侧栏底部上方分隔线 ②自启面板改中间栏内嵌展开条（列表下推不遮挡卡片） ③修复拖拽排序（插到目标行后面，之前插前面=拖相邻行不动）+按下项目行不再关闭面板（拖入面板被打断的根因） ④列表行显示标签（色点+名） ⑤运行实际端口回写 lastPort，重启 Reopen 后接管检测不丢状态（修复"运行中显示已停止"） ⑥表单标签改下拉展开+颜色用户自选（8 色板，存 settings.tagColors），标签限 6 字 | 实测反馈 |
| 2026-08-20 | 外观设置：删主题风格三卡、特殊风格改名+多色搭配（主题色+辅助色）、删紧凑间距，效仿 Telegram 设置界面 | 用户验收反馈 |
| 2026-08-20 | 引导卡不得遮挡介绍区域，加箭头指向 | 用户验收反馈 |
| 2026-08-20 | 托盘加右键菜单；图标换用户指定素材（镂空透明，resources/tray-icon.png） | 用户验收反馈 |
| 2026-08-21 | 四项整改：①删侧栏右缘 box-shadow（设置按钮上方"像阴影的线"）②布局重做：卡片固定 220 默认四列，自启面板改占一列（224 固定宽列卡片，与中间栏平级），日志抽屉改占两列（380→456），面板/抽屉挤入时项目自动让列（4→3→2→1 守恒），minWidth 1100→1160 ③卡片加拖拽（draggable+与行同款三连），卡片模式下可拖入自启面板/拖拽排序；mousedown 关面板豁免 .card ④标签彻底简化：列表/卡片/侧栏/组头不再显示标签，颜色体系全删（tagColors 字段迁移剥离、TAG_COLORS/色板/折角全删）；表单标签改空输入框+拼音联想下拉（pinyin-pro：全拼/首字母/中英文子串匹配已有标签，无匹配显示"作为新标签"候选），只有提交才真正创建标签（输入过程零持久化） | 用户拍板 |
| 2026-08-21 | 五项整改：①拖拽排序加动态占位：拖到目标上时目标后面张开空位（列表=高度动画让位、卡片=淡入空格），被拖项目半透明拖影，drop/dragend 清理 ②标签显示恢复：卡片右下角（绝对定位不撑高、Tag icon+文字、无三角）、列表行放名称后中间位置（Tag icon+文字），侧栏筛选保留 ③极端压缩自适应（container query，中间栏窄于 420px 即只剩 1 列时）：列表行只留名称+启动按钮，工具栏只留搜索/添加/自启 ④工具栏「+添加」从最右移到搜索 icon 后（原来极端压缩被裁一半）⑤表单标签：已选标签直接显示在输入框内（逗号分隔文本可编辑），点选已有标签=替换当前词+关闭下拉，提交时解析文本（只有提交才真正创建） | 用户实测反馈 |
| 2026-08-21 | 拖拽动效重做（用户要求先沟通确认，两轮问答拍板）：删占位空位（会闪、会打乱顺序观感），改 Notion/访达式「指示线+松手平滑移动」——悬停目标时目标边缘画一条不占空间的指示线（行=底边横线、卡片=右缘竖线），松手后 FLIP 动画平滑补位；列表行名称 flex:1+超长尾部渐隐（mask）、启动按钮 flex-shrink:0 永远固定最右（极端压缩也如此）；工具栏图标化压缩：排序从下拉框改图标按钮+弹出小菜单（打勾当前项），视图切换合成一个按钮（显示目标视图图标点一下切换），极端窄栏全部按钮可见 | 用户实测反馈 |
| 2026-08-21 | 用户登记「supOS-Free网页构建」文件夹（无 index.html 的杂项目录）后访问 51573 显示 Not Found——原因：web 类型临时服务把文件夹当根目录，根目录没有 index.html 即 404；正确用法是拖具体 html 文件；「根目录无 index 时列目录」兜底待用户拍板 | 用户提问 |
| 2026-08-21 | 侧栏标签右键菜单：重命名…（弹输入框，项目内标签名+染色键+当前筛选分类一起改名，重名拦截）、染色…（8 色+无色色板弹窗，默认无色）、删除标签（确认后从所有项目移除+清染色，项目本身不受影响）；颜色填进侧栏标签右侧 Tag icon（fill 实心色）；Settings 加回 tagColors 字段（上轮删除的迁移行撤销） | 用户拍板 |
| 2026-08-21 | 染色 UI 重做：删「染色…」弹窗与色板圆点（默认无色时 Ban 图标像黑色），改右键菜单内嵌渐变色板条（最左灰段=无色，红橙黄绿蓝紫粉青渐变）+ 可拖动刻度线（拖动实时预览、松手提交、键盘松开提交），尾部「无色」按钮一键清除（当前无色时按钮置灰不可点）；染色同步到项目栏：列表行与卡片右下角的 Tag icon 一起填色；修复右键菜单弹出后第一时间点不到（菜单定位下移 4px 让第一项落在光标下 + backdrop 只响应左键，macOS 触控板右键手势收尾的 button=2 click 不再误关菜单）；自启面板 mousedown 关闭忽略非左键 | 用户实测反馈 |
| 2026-08-21 | 染色滑块两处修复：选完颜色（松手提交/点无色按钮）菜单自动关闭；无色时刻度线改透明填充+描边空心圆（之前灰色实心像黑色，应与空心 icon 一致），有色时实心色+白边 | 用户实测反馈 |
| 2026-08-21 | 修复标签 icon 无色时全黑：lucide Tag 传 fill={undefined} 时 React 移除 fill 属性，SVG 默认填充黑色；三处（侧栏/列表行/卡片）改显式 fill={color ?? 'none'} | 用户实测反馈 |
| 2026-08-21 | 识别增强全部落地（docs/03 方案 S1-S9）：项目根下钻 4 层（嵌套项目/前后端分离 multi 容器）；multi 候选清单（逐个确认式：点一个→确认表单预填→提交→回清单该行打勾→完成，用户拍板）；网页入口 entryPath 贯穿（启动打开识别出的 html 而非目录根，Not Found 根治）；框架默认端口改匹配 scripts 脚本内容（vite/next/astro/umi/flask/uvicorn）；新增 Python（.venv 优先）/bun·deno（提取 .command 执行行）/启动脚本三类识别；vite 端口漂移日志解析（健康检查切换实际端口，待真机场景验证）；实测 10 案例全通过（supOS 容器 6 候选、app/workshop-twin 3000 读自 vite.config、wechat-mp 根 app.py 5001、mediastory server 3001、texpeed 3470、回归 lanzhuo-md 3459） | docs/03 方案 + 用户拍板 |
| 2026-08-21 | 网站常驻（第一步，用户拍板）：网页类型登记提交即自动上线；打开 Reopen 自动把网页项目拉回在线（服务类保持手动点启动）；端口稳定（没指定端口时沿用上次实际端口，重启后地址不变，被占自动分配）；列表行/卡片端口运行中可点开浏览器（↗ 链接）。产品讨论产出：静态成品（面包）vs 开发项目（后厨）之分；竞品调研结论（DevLaunch/Simple Dev Server Viewer/Laragon，无完整闭环竞品）。第二步「项目组」（嵌套收纳+组自启只拉成品）待做 | 用户产品讨论拍板 |
| 2026-08-21 | 项目组（第二步，用户拍板）：拖入多项目文件夹 → 弹「发现一个项目组」预览（组名可改、默认全勾、成品排最前）→ 确认登记成组，列表里一行；组行展开/收起，子项缩进收纳；标签打在组上；子项不可单独拖拽排序/不可单独自启（拖组）；组自启=开机只拉组内成品子项，开发子项保留手动启动；卡片模式组头卡（全宽、点击展开）；组抽屉显示子项列表可逐个启停/点端口；删除组连带子项（确认文案说明）；数据模型 ProjectType 加 'group' + parentId | 用户产品讨论拍板 |
| 2026-08-21 | 项目组实测整改：①组卡重做——与普通卡片同样大小（220 宽），卡内=组名+自启勾+「N 个子项 · M 个在线」+成品网站端口可点，点卡=打开组抽屉，子卡不再网格铺开（回归拍板"点开抽屉看子项"）；②识别加「成品」候选——项目含 dist/index.html 时生成「XXX成品」候选（web 类型登记即上线，点开是官网首页、站内子页面导航），解决"两个案例页被当网站、真正的 7100 官网没被识别"；③组抽屉子项行可点开子项详情（看日志） | 用户实测反馈 |
| 2026-08-21 | 二轮实测整改：①webServer 目录路径自动补 index.html——`/factory/` 这类站内子页面导航不再 404（端口打开网站不能点击跳转的根因）；②组预览默认只勾「成品网站」（web 且无 entryPath），散装 html 与开发项目默认不勾——用户目的=网站首页端口，不要素材文件端口；③子项详情抽屉左上角加返回箭头（回组视图）；④失败翻译加跨平台特征（Permission denied/Bad CPU type 等→"依赖从 Windows 拷贝，删 node_modules 重装"）——用户提 Windows 电脑做的东西要能放进来（识别本就支持，打不开是依赖二进制跨平台，成品静态站无此问题） | 用户实测反馈 |
| 2026-08-21 | **「端口打开是空的」根治**（用户愤怒反馈，逐层实测破案）：三个成品端口对 `/` 一律 Not Found，而用户手动跑的 7100（python http.server 服务同一 dist）正常——根因是 webServer.ts:46 路径拼接 `resolve(rootDir, `.${target}`)` **点后少一个斜杠**：带 `/` 的请求恰好拼成 `./xxx` 正常，首页请求 `/` 走 entryName=`index.html` 拼成 `.index.html`（隐藏文件名）→ 404 → 空白页（内联复刻脚本实锤：`GET /` 404、`GET /index.html` 正常；排除 TCC 权限/路径 Unicode 差异）。修复：`./${target}`；404 分支加 console.error 打印实际路径（盲查教训）。另加**网站端口接管**：候选端口被占时先探测是不是在响应的网站（2xx/3xx 且 HTML，1.5s 超时）——是则直接接管显示（日志「端口 X 已有网站在运行，直接接管」），不是则维持原行为；用户把「supos-free-site成品」端口填 7100 即可认领手动跑的 python 服务器，地址永远是 7100（python 没跑时 Reopen 自己绑 7100） | 用户实测反馈 |
| 2026-08-21 | **多成品点错端口真相**（用户再反馈"64215 是空的"）：实测三端口内容——64215=智造园区 3D 数字孪生（用户点错的那个）、64216=官网首页（与 7100 完全一致）、64217=车间数字孪生；文件夹里 3 个**互相独立**的网站（互不引用，官网自包含）；全文件夹搜 7100：官网代码里没有，只出现在 start-twin-preview.bat（Windows 上 workshop-twin 的开发端口）和说明文档里——**7100 是用户自己的习惯数字**。用户拍板（三轮问答）：①候选行显示网站标题（纯规则读 `<title>`，回答"纯 app 能否预知项目内容"=能，靠读磁盘文件）②**默认只勾「最大成品」**（fileCount 最多：supos-free-site=100 文件 vs 另两个=3），web 候选按 fileCount 降序排最前 ③另拍板（Phase B 待做）：同一项目合并一条+启动方式切换、Docker 自动识别、python 静态形式=内置为主+可切换真实命令 | 用户实测反馈+拍板 |
| 2026-08-21 | **Phase B 启动方式体系落地**（用户拍板"下一步先做B"）：数据模型 Project 加 launchModes（LaunchMode{id/kind/label/command/port/entryPath/staticRoot}）+activeMode，类型跟随方式（preview=web，dev/python-static/docker=service）；识别重做——buildLaunchModes 一个项目生成方式清单（成品预览排最前为默认：dist/index.html→preview；package.json→dev；requirements+app.py→python-dev；serve.ts→bun；启动脚本→launch；docker-compose.yml→docker；有成品附赠 python-static「python3 -m http.server」真实命令；兜底 html→preview），「supos-free-site」与「supos-free-site成品」**合并成一条**（preview+dev+python-static），detectMulti 删独立成品候选；启动分发——startProject 按方式走 startWeb（staticRoot=dist）/startService（spawnAndWatch 抽取共用）/startPythonStatic（真实 python 进程+接管探测+没装 python 人话提示）/startDocker（compose up+没装 Docker 人话提示）；switchLaunchMode IPC（运行中先停→记录新方式+同步类型→重启）；详情抽屉「启动方式」分段切换器；表单只读展示方式清单；组自启/自动回在线/登记即上线均改按 preview 方式+老数据按 type 兼容；10 案例检测回归全对（supOS 容器 9 候选→6 候选、官网 files=100 排第一默认勾选）。另：打开浏览器链接统一 127.0.0.1（localhost 别名偶发解析成 IPv6 ::1 连不上，127.0.0.1 永不翻译） | 用户拍板+实施 |
| 2026-08-21 | **三种启动方式三种效果破案 + python-static 服务错目录修复**（用户实测反馈"预览正常/开发服务器失败/python 有动效没图"）：①成品预览=内置服务器端 dist 成品仓（资源全齐）；②开发服务器失败=`node_modules/.bin/vite: Permission denied`——项目从 Windows/网盘拷贝，node_modules 里 Unix 脚本权限是 `rw-rw-rw-`（无执行位，实测 npm run dev 直接 exit 126），项目没坏是文件「上岗证」丢了；③python 有动效没图=**Reopen bug**：附赠 python-static 方式没写 staticRoot→python 服务了**项目根源码目录**（源码 index.html 引的 JS/CSS 根目录有→动效正常；两张门神图只有 dist/assets/gate/ 里有→图片 404）。修复：detect.ts 附赠 python-static 带 preview 的 staticRoot + projectManager.ts 老数据运行时兜底（mode.staticRoot ?? preview 的 staticRoot ?? 项目根，老数据不用重拖）；验证：typecheck/lint 全绿、检测回归 python-static 带 dist、实测 python 服务 dist 首页/两张门神图/JS 全 200。**用户产品思考（待拍板）**：要不要按「服务 vs 网页」重新划分启动方式——需先讲清分类本质（端成品 vs 跑代码）再定 | 用户实测反馈+拍板修复 |
| 2026-08-22 | **启动方式分类答疑**（用户产品思考深挖）：用户提出「要不要按服务/网页分类」——讲解两把尺子：①端成品 vs 跑代码（启动时项目代码有没有被「执行」；成品预览/python http.server 都只是递文件的搬运工，开发服务器/Python 后端/Docker 是执行代码的流水线）②有界面 vs 没界面（浏览器里有没有东西看，纯 API 才配叫服务）。「开发服务器」模糊因为两头都沾（跑着代码的网页）。用户自悟「需要激活 vs 不需要激活」=同一把尺子的用户语言（举例：iCloud 里的单 html 文件不需要激活打开——实测该文件识别=web 单 preview 方式，登记即在线，方式只有一种时不显示切换器）。**「方式副标题」方案被用户否决**（三个选项都嫌困惑，方向待用户重新表述）；**权限自动修复已拍板** | 用户产品讨论+拍板 |
| 2026-08-22 | **权限自动修复落地**（用户拍板"自动修复+日志记录"）：Windows/网盘拷贝项目的 node_modules/.bin 无执行位（rw-rw-rw-）→ `npm run dev` 报 Permission denied（exit 126）。实现：spawnAndWatch 失败时检测——exit 分支查最近日志 Permission denied 特征 + error 分支查 EACCES → fixBinPermissions（lstatSync 只处理文件跳过软链，补执行位）→ 自动重试一次（isRetry 防循环），日志透明记录「已自动补上 N 个脚本的执行权限，重试启动」。**端到端实测**（esbuild 打包 projectManager + mock store/electron 跑真项目 supos-free-site）：第一次 Permission denied → 自动补 8 个脚本 → 重试 vite 真启动 → 撞**第二层病** `Cannot find module @rollup/rollup-darwin-arm64`（Windows 上装的依赖缺 Mac 平台二进制）→ 补 failWithLogHint 正则（npm optional deps 报错特征）+文案统一「依赖没装好，删 node_modules 和 package-lock.json 重新 npm install」。实测后用户项目 .bin 已被治好（vite 权限 rwx）。三层病全貌：执行权限（可自动修）/缺平台二进制（提示重装）/端口占用（提示停旧的） | 用户拍板+实施 |
